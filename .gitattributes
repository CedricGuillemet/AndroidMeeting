# Normalize line endings.
* text=auto

# Gradle wrapper script must stay LF so it runs on Linux/macOS CI.
gradlew text eol=lf
*.sh text eol=lf

# Windows batch scripts use CRLF.
gradlew.bat text eol=crlf
*.bat text eol=crlf

# Binary.
*.jar binary
*.aar binary
*.png binary
